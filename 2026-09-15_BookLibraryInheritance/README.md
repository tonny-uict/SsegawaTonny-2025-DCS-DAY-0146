# BookLibraryInheritance - Book extends Library

Assignment: Subclass `Book` inherits from `Library` with `borrow()` method (Module 3 Inheritance).

Compile & Run:
```bash
javac Main.java && java Main
```
